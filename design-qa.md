source visual truth path: /Users/apple/work/web3.ccvar.com/public/reference-simulation-lab.png
implementation screenshot path: /Users/apple/work/web3.ccvar.com/qa-screenshot.png
vault modal screenshot path: /Users/apple/work/web3.ccvar.com/qa-vault-modal.png
live guard screenshot path: /Users/apple/work/web3.ccvar.com/qa-live-guard.png
kill switch screenshot path: /Users/apple/work/web3.ccvar.com/qa-kill-switch.png
live reconciliation screenshot path: /Users/apple/work/web3.ccvar.com/qa-live-reconcile.png
autopilot screenshot path: /Users/apple/work/web3.ccvar.com/qa-autopilot-persisted.png
autopilot steps screenshot path: /Users/apple/work/web3.ccvar.com/qa-autopilot-steps.png
live autopilot preflight screenshot path: /Users/apple/work/web3.ccvar.com/qa-live-autopilot-preflight.png
live setup checklist screenshot path: /Users/apple/work/web3.ccvar.com/qa-live-setup-checklist-ready.png
live setup mobile screenshot path: /Users/apple/work/web3.ccvar.com/qa-live-setup-mobile-visible.png
operator runbook path: /Users/apple/work/web3.ccvar.com/docs/operator-runbook.zh-CN.md
local smoke script path: /Users/apple/work/web3.ccvar.com/scripts/smoke-local.sh
acceptance env readiness script path: /Users/apple/work/web3.ccvar.com/scripts/check-acceptance-env.sh
live acceptance script path: /Users/apple/work/web3.ccvar.com/scripts/acceptance-live.sh
release verify script path: /Users/apple/work/web3.ccvar.com/scripts/verify-release.sh
final audit script path: /Users/apple/work/web3.ccvar.com/scripts/final-audit.sh
final completion gate script path: /Users/apple/work/web3.ccvar.com/scripts/assert-final-complete.sh
vault connection test screenshot path: /Users/apple/work/web3.ccvar.com/qa-vault-connection-test.png
vault connection test mobile screenshot path: /Users/apple/work/web3.ccvar.com/qa-vault-connection-test-mobile.png
responsive tablet screenshot path: /Users/apple/work/web3.ccvar.com/qa-responsive-tablet.png
responsive mobile screenshot path: /Users/apple/work/web3.ccvar.com/qa-responsive-mobile.png
responsive mobile full screenshot path: /Users/apple/work/web3.ccvar.com/qa-responsive-mobile-full.png
responsive mobile live guard screenshot path: /Users/apple/work/web3.ccvar.com/qa-responsive-mobile-live-guard.png
risk profile screenshot path: /Users/apple/work/web3.ccvar.com/qa-risk-profile.png
strategy profile screenshot path: /Users/apple/work/web3.ccvar.com/qa-strategy-profile.png
workspace export screenshot path: /Users/apple/work/web3.ccvar.com/qa-workspace-export.png
paper ledger screenshot path: /Users/apple/work/web3.ccvar.com/qa-paper-ledger.png
paper account screenshot path: /Users/apple/work/web3.ccvar.com/qa-paper-account.png
paper reset screenshot path: /Users/apple/work/web3.ccvar.com/qa-paper-reset.png
backtest screenshot path: /Users/apple/work/web3.ccvar.com/qa-backtest.png
backtest history screenshot path: /Users/apple/work/web3.ccvar.com/qa-backtest-history.png
local data screenshot path: /Users/apple/work/web3.ccvar.com/qa-local-data.png
app info screenshot path: /Users/apple/work/web3.ccvar.com/qa-app-info.png
preflight screenshot path: /Users/apple/work/web3.ccvar.com/qa-preflight.png
ai policy screenshot path: /Users/apple/work/web3.ccvar.com/qa-ai-policy.png
autopilot live plan UI screenshot path: /Users/apple/work/web3.ccvar.com/qa-autopilot-live-plan-ui.png
comparison evidence path: /Users/apple/work/web3.ccvar.com/qa-comparison.png
viewport: 1440 x 1024
comparison viewport: 1440 x 1024
state: Real-time Sim, Binance data source, Shadow mode, AI Momentum Pro selected, binary-served UI on `http://127.0.0.1:8787/`, responsive desktop/tablet/mobile workstation layout available, Backtest tab runs a 15m public-candle strategy test, Live Guard locked, Kill Switch clear after QA, Strategy Profile available from the top Strategy control, AI Autopilot visible in Simulation Controls, Paper Ledger, Paper Account summary, and Paper Reset control available in the Trades tab, Risk Profile visible in Live Guard, AI Execute validation mode visible and disabled pending recent Account Sync, Execution Ledger visible with reconciliation status, Account Sync visible, Audit Trail workspace export visible, Exchange Vault empty after QA cleanup
latest live-market screenshot path: /Users/apple/work/web3.ccvar.com/qa-screenshot.png
latest market verification: Binance and OKX `/api/lab-state` both returned 96 public 15m candles with a `Live public` market-data event.
latest simulation verification: OKX `/api/simulate/step` returned AI Decision, Risk Check Approved, and Sim Fill events; invalid exchange returns JSON error.
latest vault verification: `POST /api/credentials` rejected OKX without `apiPassphrase` at HTTP 400; OKX with `apiPassphrase` encrypted successfully and returned only `apiKeyMask`; `GET /api/credentials` did not include raw key, secret, or API passphrase; the QA credential was deleted and empty lists return `[]`.
latest vault connection test verification: Exchange Vault now includes a `Connection Test` card backed by read-only `/api/account-sync`; it selects a saved credential, accepts a vault passphrase, constrains OKX to `demo`, lets Binance choose `testnet` or `demo`, and reports can-trade, balances, open orders, snapshot id, and synced time without placing orders. Headless Chromium QA against latest assets `index-C8pDXuyA.js` and `index-Bb7jW6nx.css` verified desktop 1440 x 1024 and mobile 390 x 844 render the card, show `TEST READ-ONLY CONNECTION`, keep the button disabled without passphrase, have `modalOverflowX: 0`, `bodyOverflowX: 0`, and no detected desktop text overflow; screenshots captured `/Users/apple/work/web3.ccvar.com/qa-vault-connection-test.png` and `/Users/apple/work/web3.ccvar.com/qa-vault-connection-test-mobile.png`.
latest live guard verification: production unlock returned HTTP 400; testnet unlock with `ENABLE TESTNET LIVE` returned HTTP 200 and a session id; manual lock returned HTTP 200; `GET /api/live-guard` is locked after QA; `POST /api/live-execute` while locked returned HTTP 423; unlocked live execute without credential returned HTTP 400 and wrote `live_execute.credential`; unlocked live execute with a trade credential but no recent account snapshot returned HTTP 428 and wrote `live_execute.account`; the temporary QA credential was deleted; `GET /api/audit-log?limit=6` reports `valid: true` with 22 checked hash-chained entries.
latest kill switch verification: `GET /api/kill-switch` returned clear state; after Live Guard unlock, `POST /api/kill-switch` with `activate` returned active state and `GET /api/live-guard` returned locked; while active, `POST /api/simulate/step`, `POST /api/live-execute`, and `POST /api/autopilot` reject with HTTP 423 when applicable; `resume` cleared the halt without unlocking Live Guard; audit contains `kill_switch.activate`, `simulate_step.kill_switch`, `live_execute.kill_switch`, `autopilot.stop`, rejected `autopilot.start`, and `kill_switch.resume`; final audit verification reports `valid: true`.
latest signer/executor verification: Go tests cover Binance HMAC-SHA256 signed payloads including the official request-security sample, test-order request construction, live-exec Binance `/api/v3/order/test` submission through httptest, OKX Base64 HMAC-SHA256 REST signing, OKX demo auth headers, OKX `x-simulated-trading: 1`, and OKX validation-only signed preflight.
latest autopilot verification: `GET /api/autopilot` returned idle sanitized state; `POST /api/autopilot` with shadow mode and `maxSteps: 1` immediately ran one Binance public-market AI Decision / Risk Check / Sim Fill step and completed; the response included `runId`; `GET /api/autopilot-runs?limit=3` returned the persisted completed run; `GET /api/autopilot-steps?runId={id}` returned the persisted step with result/events JSON; a running OKX shadow Autopilot was stopped immediately by `POST /api/kill-switch` activation; while Kill Switch was active, `POST /api/autopilot` returned HTTP 423 with `kill switch is active`; Kill Switch resume restored clear state; final audit verification reports `valid: true` and includes `autopilot.start`, `autopilot.step`, `autopilot.stop`, and rejected `autopilot.start` without secrets.
latest execution ledger verification: `GET /api/live-executions?limit=6` returned `records: []` in empty state; Go tests prove risk-rejected live intents persist with `riskStatus: rejected` and `executionStatus: not_submitted`, while validated Binance execution persists with `riskStatus: approved`, `executionStatus: validated`, and nonempty intent/risk/execution JSON; missing or stale account snapshots stop before risk and do not create ledger rows.
latest reconciliation verification: `GET /api/live-reconciliations?limit=6` returned `records: []` in empty state; `POST /api/live-reconcile` without `liveExecutionId` returned HTTP 400 and wrote `live_reconcile.execution` to audit; final audit verification reports `valid: true` with 32 checked entries. Go tests cover validation-only rejection, unsubmitted rejection, credential exchange mismatch, persisted reconciliation reports, Binance signed `GET /api/v3/order`, Binance `origClientOrderId` fallback, OKX demo `GET /api/v5/trade/order`, OKX auth headers, and OKX non-demo rejection.
latest account sync verification: `GET /api/account-sync?exchange=Binance&environment=testnet&symbol=BTCUSDT` returned `snapshot: null` in empty state; production account sync returned HTTP 400 and wrote `account_sync.rejected`; live execution now requires a same credential/exchange/environment/symbol snapshot from the last five minutes before risk evaluation.
latest risk profile verification: `GET /api/risk-profile` returned the default profile with `maxOrderUsdt: 1000`; `PUT /api/risk-profile` with `maxOrderUsdt: 50` persisted a tight QA profile and wrote `risk_profile.update`; a subsequent Binance `POST /api/simulate/step` was rejected by risk with `order size 500.00 exceeds 50.00`; the original profile was restored; final audit verification reports `valid: true`.
latest strategy profile verification: `GET /api/strategy-profile` returned the default `AI Momentum Pro` profile; `PUT /api/strategy-profile` with `side: sell`, `orderSizeUsdt: 250`, `intervalSeconds: 7`, and `maxSteps: 2` persisted the QA profile and wrote `strategy_profile.update`; a subsequent `POST /api/simulate/step` generated a SELL 250 USDT intent with the QA strategy reason; `POST /api/autopilot` with only `mode: shadow` inherited side, size, interval, and maxSteps from the Strategy Profile; the original profile was restored; final audit verification reports `valid: true`.
latest AI policy verification: `Local AI Policy v0.2.0` now generates manual simulation, Shadow/Paper Autopilot, and Live Autopilot planned intents. Unit tests verify model identity, policy version, signal, confidence, TTL, feature impacts, reasoning, lower confidence for momentum-misaligned sides, Live Autopilot planner side/size forwarding, and live-exec internal planned-intent handling. `/api/lab-state` refreshes the visible AI Model Verdict from the same local policy using live public market data and Strategy Profile defaults.
latest backtest verification: `POST /api/backtest/run` with Binance `BTCUSDT`, 200 15m candles, fast window 6, and slow window 18 returned a finite summary, a persisted `runId`, 200 equity points, live-public market data source, benchmark return, max drawdown, fees, win rate, exposure, and a trade array. `GET /api/backtest-runs?limit=3` returned the saved run with sanitized result JSON. The API explicitly falls back to `marketDataSource: local seed` with a warning when public exchange candles are unavailable.
latest paper ledger verification: `POST /api/simulate/step?exchange=Binance&symbol=BTCUSDT&mode=paper` returned a simulated fill and persisted a paper execution record; `GET /api/paper-executions?limit=3` returned the new record with `mode: paper`, `source: manual`, `riskStatus: approved`, `fillStatus: filled`, intent JSON, decision JSON, fill JSON, and events JSON. A Shadow Autopilot max-step run also persisted a paper execution record with `source: autopilot` and the run id.
latest paper account verification: `GET /api/paper-account` returned starting capital, cash, equity, realized/unrealized PnL, total PnL, return percentage, fees, fill/reject counts, and simulated positions derived from paper execution records; export includes the same paper account snapshot.
latest paper reset verification: isolated QA service on `127.0.0.1:8788` used a temporary SQLite database, seeded one paper record, confirmed wrong phrase `RESET` returned HTTP 400 without deleting the record, confirmed `RESET PAPER` deleted one record, returned Paper Account to 100,000 USDT equity with zero fills, and wrote both rejected and approved `paper.reset` audit entries with final audit verification `valid: true`.
latest workspace export verification: `GET /api/export` returned service `ccvar-quant`, Strategy Profile, Risk Profile, Autopilot state/runs/steps, backtest run history, paper account snapshot, paper execution records, live execution and reconciliation arrays, audit verification `valid: true`, explicit safety exclusions, `Cache-Control: no-store`, and an attachment filename; the exported JSON did not include `apiKey`, `secret`, `passphrase`, `ciphertext`, `salt`, or `nonce` keys.
latest local data verification: isolated QA service on `127.0.0.1:8788` used a temporary SQLite database, seeded three backtest records, confirmed wrong phrase `PRUNE` returned HTTP 400, confirmed exact phrase `PRUNE LOCAL DATA` with keep-one backtest retention deleted two records and kept one, returned protected data sets for credentials, audit, live ledgers, Risk Profile, and Strategy Profile, and reported audit verification `valid: true`.
latest app diagnostics verification: `GET /api/health` returned service `ccvar-quant`, version `0.1.0`, and OK state; `GET /api/app-info` returned version, bind address `127.0.0.1:8787`, UI URL, darwin/arm64 runtime, Go version, SQLite database path/size, runbook/safety document paths with existence and byte sizes, local-only Origin guard flag, disabled production trading/account-sync flags, and Binance/OKX support without document contents or secret material; a request with `Origin: http://127.0.0.1:5173` returned HTTP 200 and echoed that origin, while `Origin: https://example.com` returned HTTP 403 with `origin not allowed`; `./bin/ccvar-quant --version` returned `0.1.0`.
latest duplicate launch verification: while the main local client was serving `127.0.0.1:8787`, `./bin/ccvar-quant --open` detected the occupied address, verified `/api/health` returned `service: ccvar-quant`, opened the existing client URL, logged the reuse decision, and exited with code 0 instead of opening SQLite or starting a competing server.
latest preflight verification: `GET /api/preflight` returned `overall: warn`, `ready: 11`, `warn: 2`, `block: 0`. Checks included ready local Origin protection, SQLite database, production gate, Kill Switch clear, Live Guard locked, Autopilot idle, audit hash ok with 60 checked entries, Risk Profile loaded, Strategy Profile `Binance / BTCUSDT`, Binance public market ready, OKX public market ready, a warning for no saved trade key, and `live_autopilot` warning with `trade key required` plus `guard locked`. Unit tests cover default warn-without-credentials behavior, Kill Switch block behavior, public-market warn behavior, missing adapter block behavior, local store/audit/profile checks, and Live Autopilot ready/warn/block aggregation.
latest UI verification: headless Chrome rendered the embedded binary UI with title `CCVar Quant Lab`, 14 canvas elements, no horizontal or vertical viewport overflow, and no detected text overflow; Strategy Profile UI QA opened the top Strategy control, verified the modal summary `BINANCE / BTCUSDT`, `BUY 500 USDT`, `15S / UNLIMITED`, found no strategy-modal, strategy-profile-body, strategy-summary, modal-header, page, or text overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-strategy-profile.png`; Backtest UI QA opened the Backtest tab, verified Ending Equity, Return, Benchmark, Max DD, Equity Curve, Trades, History, 6 saved run rows, compact `live`/`seed` source tags, History constrained to the right panel, and no chart-workspace/backtest-summary/backtest-body/backtest-table/backtest-history overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-backtest-history.png`; Risk Profile UI QA opened Live Guard, verified the `Risk Profile` card and saved status, found no live-guard-modal, live-guard-grid, credential-form, live-execute-panel, audit-list, risk-profile-card, page, or text overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-risk-profile.png`; Paper Account UI QA clicked AI STEP, opened the Trades tab, verified the account strip shows `Equity`, `Cash`, `Realized`, `Unreal.`, and `Return`, verified the latest `BTCUSDT` paper record with `BUY`, `approved`, `filled`, and no bottom panel/table/account strip/page text overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-paper-account.png`; Paper Ledger UI QA also captured `/Users/apple/work/web3.ccvar.com/qa-paper-ledger.png`; Paper Reset UI QA seeded an isolated paper record, verified wrong-phrase rejection status, accepted `RESET PAPER`, verified `Reset 1`, empty ledger text, 100,000.00 USDT equity, and no bottom-panel/account-toolbar overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-paper-reset.png`; Workspace Export UI QA opened Live Guard, verified the Audit Trail export button, triggered a JSON download with the server-provided filename, showed `Saved`, found no audit-summary, audit-list, live-guard-modal, page, or text overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-workspace-export.png`; Autopilot UI QA started a real server-side shadow run, verified the Simulation Controls card shows `AI Autopilot`, `running`, `Run #3`, `shadow`, steps, next-run clock, and `STOP AUTO`, with no controls-panel, autopilot-card, runs-panel, or left-rail overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-autopilot-persisted.png`; Live Guard modal shows `LOCKED`, `AI Execute`, validation mode, recent account sync requirement copy, disabled execute button, visible Execution Ledger reconciliation status, visible disabled Account Sync, persisted snapshot fields `Snapshot`, `Synced`, and `Persisted`, `hash ok`, and no modal, ledger, audit-list, page, or text overflow. Reconciliation UI QA used a synthetic submitted ledger fixture without altering SQLite, verified `CHECK` is disabled before vault passphrase, enabled after passphrase, posts to the reconciliation action, shows latest status `filled #99`, and captured `/Users/apple/work/web3.ccvar.com/qa-live-reconcile.png`. Kill Switch UI QA clicked top `STOP ALL`, observed `RESUME`, verified server active state, `Guard LOCK`, disabled `AI STEP`, disabled local stop as `KILL ACTIVE`, no top/control text overflow, then resumed and verified server active state false.
latest local data UI verification: Live Guard modal shows a Local Data card with six fixed metric blocks, retention summary `30/20/500/50`, protected summary `keys/audit/live`, exact phrase placeholder `PRUNE LOCAL DATA`, disabled Prune button before confirmation, no modal/grid/audit/local-data/execution-ledger/viewport overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-local-data.png`.
latest app diagnostics UI verification: Live Guard modal shows a Client card with version `0.1.0`, bind address `127.0.0.1:8787`, runtime `darwin/arm64`, DB file `ccvar_quant.db`, size `4.0 KB`, Origin `local-only`, Docs `ready` when runbook and safety contract are found, brand version `v0.1.0`, no modal/grid/audit/client/local-data/execution-ledger/viewport overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-app-info.png`.
latest preflight UI verification: Live Guard AI Execute panel shows a Preflight card with `WARN`, `Ready 11`, `Warn 2`, `Block 0`, five fixed status blocks for `Audit`, `Guard`, `Vault`, `Market`, and `Live`, no live-guard-modal/live-guard-grid/live-execute-panel/preflight-card/audit-list/client-info/local-data/execution-ledger/viewport overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-live-autopilot-preflight.png`.
latest Live Setup checklist verification: Live Guard AI Execute panel now shows a compact `Live Setup` checklist derived from local sanitized state: Vault, Passphrase, Guard, Account, Preflight, and Execute. Browser QA at 1440 x 1024 verified latest assets `index-CyYqKl5Q.js` and `index-BWZZuREs.css`, default no-key state shows `Vault missing`, `Passphrase required`, `Guard locked`, `Account sync`, `Preflight trade key required`, and `Execute locked`, `RUN AI EXECUTE` remains disabled, and no live-guard-modal/live-guard-grid/live-execute-panel/execute-state/live-setup-card/live-setup-grid/preflight-card/audit-list overflow occurs. Mobile QA at 390 x 844 verified the checklist collapses to one column with no page/modal overflow and captured `/Users/apple/work/web3.ccvar.com/qa-live-setup-checklist-ready.png` plus `/Users/apple/work/web3.ccvar.com/qa-live-setup-mobile-visible.png`.
latest operator runbook verification: `docs/operator-runbook.zh-CN.md` documents the no-production-key safety boundary, official Binance Spot Testnet and OKX Demo Trading references, no-secret local smoke test, Binance `/api/v3/order/test` validation flow, OKX Demo `x-simulated-trading: 1` flow, Live Setup acceptance gates, failure handling, and macOS/Windows data paths. `npm run smoke:local` passed against `http://127.0.0.1:8787`, verifying `/api/health`, embedded UI assets, `live_autopilot` preflight with no block, credential list redaction, and no-store sanitized workspace export.
latest live acceptance script verification: `scripts/acceptance-live.sh` provides a repeatable Binance Spot Testnet / OKX Demo Trading acceptance harness. With `CCVAR_ACCEPTANCE_EXCHANGE` unset it exits 0 with a safe skip message. When sandbox credentials are supplied it creates a temporary encrypted Vault credential, asserts credential-list redaction, runs read-only `/api/account-sync`, unlocks Live Guard with `ENABLE TESTNET LIVE`, runs guarded `/api/live-execute` in validation-only mode by default, checks the execution ledger, checks no-store sanitized workspace export, verifies audit hash state, then locks Guard and deletes the temporary credential unless keep flags are set. Non-validation demo/testnet submission requires both `CCVAR_ACCEPTANCE_VALIDATION_ONLY=false` and `CCVAR_ACCEPTANCE_ALLOW_DEMO_SUBMIT=true`.
latest acceptance env readiness verification: `scripts/check-acceptance-env.sh` and `npm run acceptance:env` report Binance/OKX sandbox credential environment readiness without printing secret values. The report lists requested exchanges, missing exchanges, accepted variable names, selected variable names, and missing requirement categories; multi-exchange checks reject generic `CCVAR_ACCEPTANCE_*` credential variables in favor of exchange-specific `BINANCE_*` and `OKX_*` names. `scripts/final-audit.sh` embeds this readiness report in `dist/final-audit/final-audit-latest.json`, and requires it to be ready before operator-provided real sandbox acceptance can run.
latest acceptance report verification: `scripts/acceptance-live.sh` writes a sanitized JSON report to `dist/acceptance/live-acceptance-latest.json` by default when `CCVAR_ACCEPTANCE_EXCHANGE` is set. The report records step status, health metadata, snapshot id, Live Guard state, execution ledger id, reconciliation status when applicable, export redaction, audit verification, and cleanup results without API keys, secrets, passphrases, ciphertext, salts, or nonces. `CCVAR_ACCEPTANCE_REPORT_PATH=none` disables report writing, and invalid exchange values now fail instead of being treated as a safe skip.
latest AI policy UI verification: headless Chrome rendered `http://127.0.0.1:8787/` with top model `Local AI Policy v0.2.0`, visible `AI Model Verdict`, `Spread Quality`, `Liquidity Depth`, and no `GPT-4.5` text in the rendered DOM; captured `/Users/apple/work/web3.ccvar.com/qa-ai-policy.png`.
latest Live Autopilot plan UI verification: the Simulation Controls Autopilot card now renders a compact `AI Live Plan` summary when `lastResult.aiPlan` is present, showing side, notional, confidence, TTL, and policy version. Browser QA at 1440 x 1024 verified no page overflow, no Controls/Autopilot card overflow, no `GPT-4.5` text, and captured `/Users/apple/work/web3.ccvar.com/qa-autopilot-live-plan-ui.png`; default idle state correctly keeps the Replay Speed and Jump controls visible until a live plan exists.
latest AI Steps UI verification: the bottom panel now exposes an `AI Steps` tab backed by `/api/autopilot-steps`, rendering persisted run.step id, intent symbol/exchange/notional, side, confidence, risk status, and outcome. Browser QA at 1440 x 1024 verified `AI STEPS (5)`, visible Binance and OKX step rows, no page overflow, no bottom-panel/autopilot-steps overflow, no cell text overflow, and captured `/Users/apple/work/web3.ccvar.com/qa-autopilot-steps.png`.
latest responsive UI verification: the fixed 1180px shell constraint has been replaced with responsive workstation breakpoints. Browser QA verified 1440 x 1024 desktop no-overflow with latest assets `index-Yr0-TATc.js` and `index-Dt5Py8fW.css`, experiment rows expose full run metadata through title/aria labels, 1024 x 900 tablet layout has two lab columns and no page/container overflow, 390 x 844 mobile layout has one lab column and no page/container overflow, all 14 chart canvases keep nonzero dimensions after API load, and mobile Live Guard opens without modal/preflight/client/local-data/execution-ledger overflow. Captured `/Users/apple/work/web3.ccvar.com/qa-responsive-tablet.png`, `/Users/apple/work/web3.ccvar.com/qa-responsive-mobile.png`, `/Users/apple/work/web3.ccvar.com/qa-responsive-mobile-full.png`, and `/Users/apple/work/web3.ccvar.com/qa-responsive-mobile-live-guard.png`.
latest packaging verification: `go test ./...`, `npm run build:app`, `GOOS=darwin GOARCH=arm64 go build`, and `GOOS=windows GOARCH=amd64 go build` all completed; the binary served both `/` and `/api/health` on `127.0.0.1:8787`. `npm run package:desktop` created `dist/desktop/CCVar Quant Lab.app`, `ccvar-quant-lab-macos-arm64.zip`, `CCVar Quant Lab Windows x64/`, and `ccvar-quant-lab-windows-amd64.zip`; macOS `Info.plist` passed `plutil -lint`, the packaged macOS binary returned `0.1.0` for `--version`, `file` reports Mach-O arm64 and PE32+ Windows x86-64 binaries, and the zip listings contain no `__MACOSX` or `._` auxiliary files. Desktop packaging now builds release binaries with `-trimpath -buildvcs=false`, normalizes package staging timestamps, feeds sorted file lists to `zip -X`, uses a deterministic manifest timestamp by default with `SOURCE_DATE_EPOCH` / `CCVAR_RELEASE_GENERATED_AT` overrides, and consecutive `npm run package:desktop` runs produced identical `SHA256SUMS.txt` and `release-manifest.json`.
latest desktop release manifest verification: `scripts/package-desktop.sh` now removes `.DS_Store` files before and after zipping, then generates `dist/desktop/SHA256SUMS.txt` and `dist/desktop/release-manifest.json` using Node crypto. The manifest records product, version `0.1.0`, manifest version, generated timestamp, artifact filenames, byte sizes, SHA-256 hashes, critical package file paths/sizes/SHA-256 hashes for README files, launchers, bundled docs, and macOS `Info.plist`, local app URL, disabled production trading flag, and supported private sandbox environments without any local absolute paths or secrets. `scripts/verify-release.sh` recalculates every artifact and critical-file hash before release success.
latest release verifier verification: `scripts/verify-release.sh` runs Go tests, rebuilds desktop packages twice and fails if `SHA256SUMS.txt` or `release-manifest.json` differs, validates `SHA256SUMS.txt`, validates `release-manifest.json`, rejects `.DS_Store` and zip auxiliary files, checks macOS app metadata and binary architecture when local tools are available, builds the local runtime binary, starts an isolated client on `127.0.0.1:8790` with a temporary SQLite database, then runs `npm run smoke:local` and `npm run acceptance:live` skip mode against that temporary runtime before cleaning it up.
latest loopback mock acceptance verification: release verification now enables private exchange mock routing only with `CCVAR_ENABLE_LOOPBACK_EXCHANGE_MOCKS=true` and loopback-only `CCVAR_BINANCE_PRIVATE_MOCK_URL` / `CCVAR_OKX_PRIVATE_MOCK_URL` values. Startup rejects mock URLs on remote hosts, URLs with paths, query strings, fragments, or userinfo, and URLs set without the explicit enable flag. `scripts/verify-release.sh` starts a local mock exchange on `127.0.0.1:8791`, boots an isolated client with those private mock URLs, runs no-secret acceptance skip, then runs full Binance validation-only acceptance with fake testnet credentials through Vault -> Account Sync -> Live Guard -> `/api/v3/order/test` -> ledger/export/audit, runs full OKX validation-only acceptance with fake demo credentials through Vault -> Account Sync -> Live Guard -> signed-preflight -> ledger/export/audit, and verifies both sanitized reports.
latest final audit verification: `scripts/final-audit.sh` runs shell syntax checks, delegates to `npm run verify:release`, smoke-tests the current `127.0.0.1:8787` client when available, optionally runs operator-provided Binance and OKX sandbox acceptance via `CCVAR_FINAL_AUDIT_REAL_EXCHANGES=Binance,OKX`, records per-exchange results/missing exchanges, rejects generic credential variables during multi-exchange real acceptance, and writes a sanitized machine-readable handoff report to `dist/final-audit/final-audit-latest.json` without API keys, secrets, passphrases, ciphertext, salts, or nonces.
latest completion gate verification: `scripts/assert-final-complete.sh` reads `dist/final-audit/final-audit-latest.json`, verifies shell/release/current-instance gates, release artifact hashes, doc evidence, no-production trading evidence, sandbox credential readiness, exchange-specific credential variable use, and requires both Binance and OKX real sandbox acceptance reports to be present, sanitized, and passed before it prints completion success. With the current no-key final audit report it fails by design and lists missing `Binance` and `OKX` plus the exact command shape needed to collect those external proofs.
latest desktop README verification: packaged macOS and Windows README files now state the local URL, data paths, log paths, environment overrides, checksum workflow, production/mainnet disabled boundary, Binance Spot Testnet/Demo and OKX Demo Trading support, local Vault encryption warning, and withdrawal rejection. `scripts/verify-release.sh` checks the top-level macOS README, macOS app resource README, and Windows README for those critical safety phrases, verifies the Windows launcher writes to `%APPDATA%\CCVar Quant Lab\logs\client.log` through `LOG_PATH` redirection, then performs a final `.DS_Store` cleanup/check before reporting release success.
latest packaged docs verification: desktop packages now ship `docs/operator-runbook.zh-CN.md` and `docs/safety.md`; the macOS app also embeds the same docs under `Contents/Resources/docs`. `scripts/verify-release.sh` verifies the expanded package directories contain those docs with expected document markers and verifies both macOS and Windows zip listings include the packaged docs.

**Findings**
- No actionable P0/P1/P2 findings remain.

**Required Fidelity Surfaces**
- Fonts and typography: Implementation uses IBM Plex Sans and IBM Plex Mono to match the compact quant workstation feel. Numeric columns use mono typography and maintain readable hierarchy at 1440 x 1024. No negative letter spacing is used.
- Spacing and layout rhythm: The three-column lab layout, top status strip, chart workspace, AI verdict inspector, experiment list, simulation controls, bottom performance/log region, Exchange Vault, and Live Guard modal match the selected Simulation Lab structure.
- Colors and visual tokens: Dark neutral graphite base, teal AI accents, green/red trading values, amber warnings, and red stop controls align with the source direction without drifting into a one-note purple/blue palette.
- Image quality and asset fidelity: The design does not depend on decorative raster assets. Market/equity visuals are rendered with Lightweight Charts and are nonblank in the implementation screenshot. Icons come from an installed icon library rather than custom SVG assets.
- Copy and content: App-specific text is realistic trading/product UI content: experiment runs, model verdict, features, performance metrics, positions, orders, audit events, and guarded live controls. No marketing or tutorial copy is visible in the app surface.

**Open Questions**
- None from the current visual QA pass.

**Implementation Checklist**
- Build passes with `npm run build:app`.
- Go tests pass with `go test ./...`.
- macOS arm64 and Windows amd64 binaries build successfully.
- `npm run package:desktop` creates a macOS `.app`, macOS zip, Windows x64 portable folder, and Windows zip with launchers that use OS-appropriate local SQLite data paths and open the browser automatically.
- API health responds at `http://127.0.0.1:8787/api/health`.
- Client diagnostics respond at `http://127.0.0.1:8787/api/app-info`, version can be printed with `--version`, and runtime flags support custom bind address, SQLite path, and optional browser launch.
- Duplicate desktop/`--open` startup reuses only an already-running CCVar client confirmed by `/api/health`, opens that existing URL, and exits cleanly without touching SQLite.
- Preflight responds at `http://127.0.0.1:8787/api/preflight`, summarizes ready/warn/block checks, warns for no saved trade key, verifies Binance/OKX public market connectivity concurrently, includes the `live_autopilot` aggregate readiness gate, and appears in the Live Guard AI Execute panel.
- Local API CORS rejects non-local webpage origins while allowing `127.0.0.1`, `localhost`, and `::1` development/runtime origins.
- Embedded local UI renders at `http://127.0.0.1:8787/`.
- Charts render nonblank in the 1440 x 1024 screenshot.
- Responsive workstation layout renders without page/container overflow at 1440 x 1024, 1024 x 900, and 390 x 844.
- Experiment run rows expose full strategy/run/status/last-run metadata through title and aria labels while preserving dense ellipsized columns.
- Simulation controls, mode toggles, tabs, run selection, stop/pause controls, and event filters are interactive.
- Exchange Vault modal opens from the Connection strip, saves encrypted credentials, lists masked keys, blocks withdrawal permission, requires OKX API passphrase, runs a read-only testnet/demo connection test through Account Sync, and deletes saved credentials.
- Strategy Profile modal opens from the top Strategy control, persists AI intent defaults, and drives manual simulation plus Autopilot defaults.
- Live Guard modal opens from the Mode strip, blocks production unlock, allows only testnet/demo unlock with the exact phrase, locks manually, and shows hash-chain audit verification.
- Workspace Export downloads a no-store sanitized JSON package from the Live Guard Audit Trail and excludes credentials, passphrases, secrets, and credential encryption materials.
- Top `STOP ALL` calls the server-side Kill Switch, which locks Live Guard, blocks simulation and guarded execution, and requires an explicit resume before AI can continue.
- Risk Profile persists local AI risk limits in SQLite, exposes `GET/PUT /api/risk-profile`, writes updates to audit, and is used by simulation, Autopilot, and guarded live execution.
- Strategy Profile persists AI intent defaults in SQLite, exposes `GET/PUT /api/strategy-profile`, writes updates to audit, and is used by simulation and Autopilot.
- Local AI Policy generates auditable simulation/Shadow/Paper/Live Autopilot intents with model identity, policy version, signal, confidence, TTL, feature impacts, and reasoning without calling an external model provider.
- Backtest runs a local moving-average strategy over public or explicitly marked local-seed candles, exposes `POST /api/backtest/run` and `GET /api/backtest-runs`, persists sanitized history to SQLite, shows summary/equity/trades/history in the Backtest tab, is included in workspace export, and never touches credentials or order ledgers.
- AI Autopilot starts/stops server-side automatic AI steps, runs an immediate first step, polls sanitized state, appends latest events without duplication, and is stopped/rejected by Kill Switch.
- Live Autopilot account-syncs first, builds a local AI live plan from public market data, sanitized account state, and Strategy Profile defaults, then forwards the planned intent and AI trace into guarded live execution.
- Simulation Controls surfaces the latest Live Autopilot `aiPlan` as side, size, confidence, TTL, and policy version without exposing credentials or passphrases.
- AI Autopilot persists run summaries and step result/event JSON to SQLite, exposed through `GET /api/autopilot-runs` and `GET /api/autopilot-steps`.
- The bottom panel `AI Steps` tab displays persisted Autopilot step history for local audit without requiring a workspace export.
- Paper Ledger persists manual AI STEP and Shadow/Paper Autopilot simulation records to SQLite, exposes `GET /api/paper-executions`, appears in the Trades tab, and is included in workspace export.
- Paper Account derives cash, equity, realized/unrealized PnL, fees, return percentage, and simulated positions from paper executions, exposes `GET /api/paper-account`, appears in the Trades tab account strip, and is included in workspace export.
- Paper Reset clears only local paper/shadow execution records after the exact `RESET PAPER` phrase, rejects while AI is running, writes approved/rejected attempts to audit, and refreshes the Trades tab account state.
- Local Data reports SQLite table counts, prunes only older research/simulation records after the exact `PRUNE LOCAL DATA` phrase, rejects while Autopilot is running, writes approved/rejected attempts to audit, and protects credentials, audit entries, live ledgers, Risk Profile, and Strategy Profile.
- AI Execute panel opens inside Live Guard, defaults to validation-only mode, requires an unlocked guard and saved trade credential, and surfaces risk/execution result state.
- Live Setup checklist opens inside Live Guard, summarizes Vault, passphrase, Guard, Account Sync, preflight, and Execute readiness without rendering secrets, and keeps AI Execute disabled until all local gates needed for a guarded attempt are satisfied.
- Execution Ledger opens inside Live Guard, reads `GET /api/live-executions`, and shows latest structured intent/risk/execution records without secrets.
- Execution reconciliation opens inside Live Guard, reads `GET /api/live-reconciliations`, enables `CHECK` only after a vault passphrase for submitted non-validation rows, calls `POST /api/live-reconcile`, and shows latest status without secrets.
- Account Sync opens inside Live Guard, requires a saved credential and vault passphrase for new reads, rejects production/mainnet, reads only testnet/demo balances and open orders, persists sanitized snapshots in SQLite, restores the latest snapshot through `GET /api/account-sync`, and writes sync outcomes to audit.
- Cold start renders local seed data immediately instead of blocking on exchange network latency.
- Exchange switch reloads `/api/lab-state?exchange={Binance|OKX}&symbol=BTCUSDT`.
- AI STEP control calls `/api/simulate/step` and prepends AI Decision / Risk Check / Sim Fill events.
- Binance/OKX public adapters are covered by httptest parser tests.
- Binance/OKX private signing and guarded execution helpers are covered by unit tests.
- Market service fallback is covered by unit tests.
- Simulation runner intent -> risk -> fill path is covered by unit tests.
- Simulation runner Risk Profile provider rejection is covered by unit tests.
- Simulation runner Strategy Profile provider side/size/reason behavior is covered by unit tests.
- Local AI Policy intent generation, feature trace, confidence scoring, market-service verdict refresh, Live Autopilot planner forwarding, and live-exec planned-intent handling are covered by unit tests.
- Simulation runner Paper Ledger record conversion is covered by unit tests.
- Backtest long/short momentum results, not-enough-candles rejection, SQLite history round trip, and invalid history JSON rejection are covered by unit tests.
- Paper Account cash/equity/PnL and long/short position calculations are covered by unit tests.
- Paper Reset atomic delete plus hash-chain audit append is covered by unit tests and API/UI QA against an isolated SQLite database.
- Local Data pruning retention, protected live ledgers, protected audit entries, protected credentials/profile data, and hash-chain audit append are covered by unit tests and API/UI QA against an isolated SQLite database.
- Autopilot start, immediate step, max-step completion, stop, Kill Switch rejection, live account-sync-before-execute path, local AI live planning, planned intent forwarding, and passphrase redaction are covered by unit tests.
- Autopilot SQLite run/step round trip and invalid step JSON rejection are covered by unit tests.
- Paper execution SQLite round trip and invalid JSON rejection are covered by unit tests.
- Vault encryption/decryption, OKX passphrase requirement, wrong passphrase rejection, weak passphrase rejection, withdrawal rejection, and SQLite credential round trip are covered by unit tests.
- Audit hash-chain append, verification, and tamper detection are covered by unit tests.
- Live Guard unlock, production rejection, bad phrase rejection, lock, and expiry behavior are covered by unit tests.
- Live execution locked-guard rejection, missing-credential rejection, risk rejection, Binance signed test-order call, OKX demo auth submit, and OKX validation-only preflight are covered by unit tests.
- Live execution persisted Risk Profile provider rejection is covered by unit tests.
- Live execution ledger SQLite round trip, invalid JSON rejection, risk-rejected persistence, and validated-order persistence are covered by unit tests.
- Live reconciliation SQLite round trip, invalid JSON rejection, validation-only rejection, unsubmitted rejection, credential exchange mismatch, persisted report/audit, Binance signed order query, Binance client-order fallback, and OKX demo order query are covered by unit tests.
- Account sync production rejection, missing credential rejection, vault decryption path, audit writes, SQLite snapshot persistence/latest lookup, Binance signed account/open-orders reads, and OKX demo balance/pending-orders reads are covered by unit tests.
- Client startup config parsing, version flag, local Origin guard, CORS handler behavior, browser address normalization, and `/api/app-info` payload construction are covered by unit tests.
- `/api/app-info` reports packaged operator runbook and safety-contract availability with document paths, existence flags, and byte sizes while keeping document contents out of the response.
- Preflight report construction, no-credential warning, Kill Switch block, public-market warning, missing-adapter block, Live Autopilot aggregate readiness, and store/audit/profile checks are covered by unit tests.
- Desktop packaging is covered by a repeatable script that rebuilds UI assets, compiles macOS arm64 and Windows amd64 binaries, generates launcher wrappers with OS-specific data/log paths, bundles the operator runbook and safety contract, validates macOS app metadata through QA, and creates clean zip files.
- Desktop release integrity is covered by reproducible macOS/Windows zip hashes, generated `SHA256SUMS.txt`, and `release-manifest.json` files for zip artifacts plus critical package files.
- Full release verification is covered by `npm run verify:release`, which validates tests, packages, package README safety text, checksums, manifest, zip cleanliness, isolated runtime smoke, acceptance skip, and Binance/OKX loopback mock full acceptance without touching the default local database.
- Final delivery audit is covered by `npm run audit:final`, which wraps release verification, sandbox credential env readiness, optional current-instance smoke, optional per-exchange Binance/OKX real sandbox acceptance, and a sanitized JSON handoff report.
- Final completion is gated by `npm run audit:complete`, which fails until the final audit report proves both Binance and OKX real sandbox acceptance passed.
- Local smoke is covered by `npm run smoke:local`, which checks health, `/api/app-info` doc diagnostics, app-info redaction, embedded assets, preflight/live_autopilot, credential-list redaction, and sanitized no-store workspace export without using exchange credentials.
- Testnet/demo acceptance is covered by `npm run acceptance:live`, which safely skips without credentials, fails on invalid exchange values, emits a sanitized JSON report when configured to run, and runs the guarded Vault -> Account Sync -> Live Guard -> validation-only execute -> ledger/export/audit chain when Binance Spot Testnet or OKX Demo Trading credentials are explicitly provided.
- The Chinese operator runbook covers Binance Spot Testnet, OKX Demo Trading, Live Setup acceptance, common failure recovery, and desktop data paths.
- Live Guard Client diagnostics visually confirm `DOCS ready` on desktop and mobile with screenshots `qa-app-info-docs.png` and `qa-app-info-docs-mobile.png`.
- Latest release artifacts:
  - macOS arm64 zip SHA-256 `f049f587ea3ff13faeea2b5c761d53307d6438b1580ecf83fff87e35429a55bf`
  - Windows amd64 zip SHA-256 `84f2209a58df1a5270449dea6c99c50c377f29ce67266767b4c8ca849ed92aec`
  - Final audit report: `dist/final-audit/final-audit-latest.json`

**Follow-up Polish**
- None from the current responsive QA pass.

patches made since previous QA pass:
- Added immutable hash-chained audit log storage and verification.
- Added Live Guard state machine for testnet/demo unlock, production rejection, TTL expiry, manual lock, and audit writes.
- Added Live Guard UI modal with unlock gate and audit trail.
- Added server-side Kill Switch API, audit entries, Live Guard lock integration, simulation-step rejection, live-execution rejection, and top-bar STOP ALL wiring.
- Added SQLite Risk Profile persistence, `GET/PUT /api/risk-profile`, audit writes, and dynamic risk provider wiring for simulation and guarded live execution.
- Added Live Guard Risk Profile UI card with editable max order, exposure, drawdown, confidence, spread, and consecutive-loss limits.
- Added SQLite Strategy Profile persistence, `GET/PUT /api/strategy-profile`, audit writes, dynamic strategy provider wiring for simulation and Autopilot defaults, and a top-bar Strategy Profile modal.
- Added `Local AI Policy v0.2.0` for auditable local intent generation, AI trace output, dynamic lab verdict refresh, and shared manual simulation / Shadow-Paper-Live Autopilot policy use.
- Added Live Autopilot AI planning before guarded execution, including sanitized `aiPlan` state, internal planned-intent forwarding, live execution AI trace output, and tests proving external JSON cannot set the internal plan path.
- Added compact Live Autopilot plan UI in Simulation Controls, replacing replay controls only when a sanitized `aiPlan` exists.
- Added server-side AI Autopilot controller for shadow/paper/live automatic steps, immediate first run, max-step completion, stop, sanitized state, audit writes, and Kill Switch integration.
- Added `GET/POST /api/autopilot` and Autopilot UI card in Simulation Controls.
- Added SQLite Autopilot run/step persistence plus `GET /api/autopilot-runs` and `GET /api/autopilot-steps`.
- Added UI Autopilot step ledger backed by `/api/autopilot-steps`, with compact risk/outcome history for AI simulated and guarded runs.
- Added persisted Autopilot run id display in the Simulation Controls card and tightened the left rail to remove overflow.
- Added `GET/POST /api/live-guard` and `GET /api/audit-log`.
- Added OKX API passphrase support inside the encrypted credential payload.
- Added Binance signed request helpers for test order validation.
- Added OKX signed REST request helpers with demo trading headers.
- Added guarded `/api/live-execute` for AI intent -> Live Guard -> Vault -> Risk -> Binance/OKX testnet/demo execution.
- Added AI Execute panel inside Live Guard.
- Added structured SQLite live execution ledger and `GET /api/live-executions`.
- Added Execution Ledger panel inside Live Guard and reduced visible audit rows to avoid hidden right-column overflow.
- Added read-only `/api/live-reconcile` and `GET /api/live-reconciliations` for submitted testnet/demo execution order-status checks.
- Added SQLite live reconciliation records with sanitized status, filled notional, order ids, report JSON, and audit writes.
- Added Binance/OKX signed read-only order reconciliation clients and tests.
- Added Live Guard Execution Ledger CHECK action, reconciliation status display, and no-overflow QA screenshot.
- Added read-only `/api/account-sync` for Binance Spot Testnet/Demo and OKX Demo Trading balance/open-order snapshots.
- Added SQLite account snapshot persistence and `GET /api/account-sync` latest snapshot restore.
- Added Account Sync panel inside Live Guard with disabled safe state and sanitized account summary.
- Added Snapshot/Synced/Persisted fields to the Live Guard account summary.
- Added account-snapshot-driven live execution preflight: missing, stale, or non-tradable snapshots are rejected before risk; approved preflight converts snapshot balances/open orders into risk account state.
- Added no-secret workspace export API and Audit Trail download control for Strategy Profile, Risk Profile, Autopilot history, execution ledger, reconciliation records, and audit verification.
- Added lightweight Backtest engine, `POST /api/backtest/run`, `GET /api/backtest-runs`, SQLite backtest history persistence, workspace export inclusion, public-candle/local-seed source labeling, Backtest tab summary/equity/trades/history UI, and no-overflow QA screenshot.
- Added Paper Ledger persistence, `GET /api/paper-executions`, workspace export inclusion, and Trades tab rendering for manual AI STEP plus Shadow/Paper Autopilot records.
- Added Paper Account derived snapshot, `GET /api/paper-account`, workspace export inclusion, and Trades tab account summary for cash/equity/PnL/fees/return.
- Added audited Paper Reset with `POST /api/paper-executions/reset`, exact phrase confirmation, Autopilot/running-AI guardrails, Trades tab reset control, and isolated QA screenshot.
- Added Local Data retention with `GET /api/local-data`, audited `POST /api/local-data/prune`, exact phrase confirmation, Autopilot/running-AI guardrails, protected live/audit/credential/profile tables, and isolated QA screenshot.
- Added client startup diagnostics with `--addr`, `--db`, `--open`, `--version`, `GET /api/app-info`, local-only Origin enforcement, Live Guard Client card, and no-overflow QA screenshot.
- Added packaged document runtime diagnostics to `/api/app-info`, smoke validation, Live Guard Client `DOCS ready` UI, and desktop/mobile QA screenshots.
- Added `npm run audit:final` for final delivery audits with release verification, current-instance smoke, optional real sandbox acceptance, and a sanitized handoff report.
- Added `npm run audit:complete` as the final completion gate for release artifacts, safety evidence, and mandatory Binance/OKX real sandbox acceptance proof.
- Added `npm run acceptance:env` for no-secret Binance/OKX sandbox credential environment readiness checks, and embedded that readiness report in final audits.
- Added duplicate-launch reuse for `--open`/desktop startup when `/api/health` confirms the address is already served by CCVar Quant.
- Added runtime preflight with `GET /api/preflight`, concurrent Binance/OKX public-market checks, ready/warn/block aggregation, Live Guard Preflight card, and no-overflow QA screenshot.
- Added Live Autopilot aggregate preflight readiness for Guard, Vault, Account Sync, market connectivity, and profile gates, plus a fifth `Live` status block in the Live Guard Preflight card.
- Added a Live Setup checklist inside Live Guard for Vault, passphrase, Guard, Account Sync, preflight, and Execute readiness, with desktop and mobile no-overflow QA screenshots.
- Added `docs/operator-runbook.zh-CN.md` for testnet/demo operator acceptance and `npm run smoke:local` for no-secret local delivery checks.
- Added responsive desktop/tablet/mobile workstation breakpoints, removing the fixed 1180px shell constraint below desktop widths while preserving the original three-column desktop layout.
- Added full experiment-run title and aria labels for dense ellipsized strategy rows.
- Added repeatable desktop packaging with `npm run package:desktop`, a macOS `.app` launcher, Windows x64 portable launcher, OS-specific default data directories, companion README files, and clean zip artifacts.
- Added deterministic desktop packaging with normalized staging timestamps, sorted zip file lists, `zip -X`, deterministic manifest timestamp support, Go `-trimpath -buildvcs=false`, and release verifier checks for consecutive package hash plus manifest stability.
- Disabled AI Execute in the UI until a recent persisted account snapshot is loaded.
- Tightened Experiment Runs column sizing to remove text overflow in the 1440 x 1024 QA viewport.
- Rebuilt embedded React UI into the Go binary.
- Regenerated QA screenshots and comparison board.

final result: passed
